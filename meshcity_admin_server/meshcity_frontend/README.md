# meshcity_frontend

Public broadcast frontend (read-only) for:
- world map (without edit tools)
- players list
- player action logs

## Run

1. Create `.env` from `.env.example`
2. Set `BACKEND_URL` to deployed `meshcity_backend`
3. Start:

```powershell
npm start
```
